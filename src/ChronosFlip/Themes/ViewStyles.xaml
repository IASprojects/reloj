<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Shared per-view header (feature 08, B5). Content = title text; Tag =
         optional action slot. Pure XAML resource — no per-view code. -->

    <Style x:Key="ViewTitleText" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource TextOnCardBrush}" />
        <Setter Property="FontFamily" Value="{StaticResource SpaceMonoFont}" />
        <Setter Property="FontSize" Value="16" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="CharacterSpacing" Value="60" />
    </Style>

    <Style x:Key="ViewHeader" TargetType="ContentControl">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ContentControl">
                    <Grid ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0"
                                   Text="{TemplateBinding Content}"
                                   Style="{StaticResource ViewTitleText}"
                                   VerticalAlignment="Center" />
                        <ContentPresenter Grid.Column="2"
                                          Content="{TemplateBinding Tag}"
                                          VerticalAlignment="Center" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>